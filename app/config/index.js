const submissionConfig = require('./submission')
const messagingConfig = require('./messaging')
const storageConfig = require('./storage')
const databaseConfig = require('./database')

module.exports = {
  submissionConfig,
  messagingConfig,
  storageConfig,
  databaseConfig
}
