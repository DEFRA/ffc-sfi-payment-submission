{{- include "ffc-helm-library.container-config-map" (list . "ffc-sfi-payment-submission.config-map") -}}
{{- define "ffc-sfi-payment-submission.config-map" -}}
data:
  NODE_ENV: {{ quote .Values.environment }}
  PORT: {{ quote .Values.container.port }}
  APPINSIGHTS_CLOUDROLE: {{ quote .Values.name }}
{{- end -}}
